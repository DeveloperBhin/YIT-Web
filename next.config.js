/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export", // replaces `next export`
  distDir: "out",   // tell Next.js where to place the exported files
};

export default nextConfig;
